import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aifitnesstracker.app',
  appName: 'AI Fitness Tracker',
  webDir: 'out'
};

export default config;
